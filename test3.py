width = float(input())
length = float(input())
depth = float(input())

volume = width * length * depth

time_seconds = volume * 15

time_minutes = time_seconds / 60

print(f"ต้องใช้เวลาในการเติมนำ้ {241.95} นาที.")
